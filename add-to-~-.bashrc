# .bashrc
 
# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

alias emacs='emacs -nw'
alias gcc='gcc -Wall -o'
alias k1411053='ssh k1411053@sol.edu.cc.uec.ac.jp'
alias rm='rm -iv'
alias cp='cp -iv'
alias mv='mv -iv'
 
# User specific aliases and functions